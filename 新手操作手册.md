# 校园论坛 - 新手操作手册

## 一、每天怎么启动项目？

每次写代码前，你需要做一件事：启动后端服务器。

### 步骤

1. 打开终端（VSCode 里按 ctrl+` 打开内置终端）
2. 输入以下命令：

```bash
cd /Volumes/Formac/code/ceshi/server
node server.js
```

3. 看到以下输出说明成功了：

```
=================================
  校园论坛后端已启动！
  地址: http://localhost:3000
=================================
```

4. 打开浏览器访问 http://localhost:3000

### 常见问题

**Q: 关掉终端后页面打不开了？**
A: 对！终端关掉 = 服务器关掉。开发时保持终端开着就行。

**Q: 改了前端代码需要重启吗？**
A: 不需要！直接刷新浏览器就行。后端会自动读取最新的前端文件。

**Q: 改了后端代码需要重启吗？**
A: 需要！按 ctrl+C 停掉，再运行 `node server.js`。

---

## 二、项目文件结构（你只需要记住这些）

```
ceshi/
├── frontend/              ← 前端（用户看到的部分）
│   ├── *.html             ← 页面（8个网页）
│   ├── css/style.css      ← 样式（颜色、布局、大小）
│   └── js/
│       ├── api.js         ← 【重要】前后端通信的桥梁
│       ├── app.js         ← 公共组件（导航栏、帖子卡片）
│       ├── index.js       ← 首页逻辑
│       ├── auth.js        ← 登录注册逻辑
│       ├── post.js        ← 帖子详情逻辑
│       ├── profile.js     ← 个人中心逻辑
│       ├── search.js      ← 搜索逻辑
│       └── category.js    ← 板块逻辑
│
├── server/                ← 后端（用户看不到的部分）
│   ├── server.js          ← 服务器入口（启动文件）
│   ├── database.js        ← 数据库（建表、初始数据）
│   ├── forum.db           ← 数据文件（自动生成，别手动改）
│   └── routes/
│       ├── auth.js        ← 登录注册的接口
│       ├── posts.js       ← 帖子的接口
│       └── comments.js    ← 评论的接口
```

### 记忆口诀

- 前端 frontend = 用户**看得到**的东西（页面、按钮、颜色）
- 后端 server = 用户**看不到**的东西（数据处理、数据库）
- api.js = 前后端之间的**快递员**

---

## 三、前后端交互原理（最核心的概念）

### 用餐厅来理解

```
你（用户）在餐厅点菜

前端 = 菜单 + 你的桌子
  - 你看菜单（浏览页面）
  - 你写下订单（填写表单）

api.js = 服务员
  - 你告诉服务员你要什么（调用 api.js 的函数）
  - 服务员把订单送到厨房

后端 = 厨房
  - 厨师（server.js）收到订单
  - 根据菜品类型分配给不同的厨师：
    - auth.js 厨师 → 负责登录注册
    - posts.js 厨师 → 负责帖子
    - comments.js 厨师 → 负责评论

database.js = 冰箱/仓库
  - 厨师从冰箱拿食材（读取数据）
  - 做好的菜放回冰箱（保存数据）
  - forum.db = 冰箱里实际的食材
```

### 用代码来理解

前端想获取帖子列表：

```javascript
// 前端 index.js 说："我要帖子列表"
const result = await getPosts(1, 5, 'latest');

// 这句代码实际做的事情（api.js 里定义的）：
fetch('http://localhost:3000/api/posts?page=1&pageSize=5&sort=latest')

// 请求发到了后端 → posts.js 处理 → 查询数据库 → 返回结果
```

### 数据流向图

```
获取数据（读）：
用户点击 → 前端 JS → api.js(fetch GET) → 后端路由 → 数据库 SELECT → 返回 JSON → 前端渲染

保存数据（写）：
用户填表 → 前端 JS → api.js(fetch POST) → 后端路由 → 数据库 INSERT → 返回结果 → 前端提示成功
```

---

## 四、日常维护：常见修改场景

### 场景1：想改页面颜色/样式

只改一个文件：`frontend/css/style.css`

```css
/* 找到 :root 部分，修改颜色值即可 */
:root {
  --primary: #1a73e8;     /* 改这个数字可以换主色调 */
  --bg: #f5f6f7;           /* 改这个换背景色 */
}
```

### 场景2：想加一个新板块

只改一个文件：`frontend/js/api.js`

找到 CATEGORIES 数组，加一项：

```javascript
const CATEGORIES = [
  { id: 'study', name: '学习交流', icon: '📖', desc: '课程讨论' },
  // ... 已有的板块 ...
  { id: 'sports', name: '体育运动', icon: '⚽', desc: '运动健身、赛事讨论' },  // ← 新增
];
```

### 场景3：想修改帖子列表每页显示数量

改 `frontend/js/index.js` 第3行：

```javascript
const PAGE_SIZE = 5;  // 改成 10 就每页显示10条
```

### 场景4：想添加一个新的 API 接口

需要改两个地方：

**第一步：后端** `server/routes/posts.js` 添加路由
```javascript
// 比如添加一个"获取帖子总数"的接口
router.get('/count', (req, res) => {
  const result = db.prepare('SELECT COUNT(*) as total FROM posts').get();
  res.json({ total: result.total });
});
```

**第二步：前端** `frontend/js/api.js` 添加调用函数
```javascript
async function getPostCount() {
  return request('/posts/count');
}
```

**第三步：** 在需要用的页面 JS 里调用这个函数

### 场景5：想添加一个全新页面

需要创建：
1. `frontend/xxx.html` — 页面结构
2. `frontend/js/xxx.js` — 页面逻辑
3. 如果需要新 API → `server/routes/` 下添加路由
4. 在 `server/server.js` 里注册新路由

---

## 五、调试技巧

### 前端调试

1. 浏览器按 F12 打开开发者工具
2. 切到 Console（控制台）标签 → 看 JS 报错
3. 切到 Network（网络）标签 → 看 API 请求是否成功
   - 红色 = 失败，点击查看原因
   - 200 状态码 = 成功
   - 400/404/500 = 失败

### 后端调试

终端窗口会显示服务器日志。加 console.log 打印调试：

```javascript
router.get('/', (req, res) => {
  console.log('收到请求，参数：', req.query);  // ← 加这行看请求参数
  // ... 原有逻辑
});
```

### 数据库调试

用 VSCode 安装 "SQLite Viewer" 插件，可以直接打开 `server/forum.db` 查看数据。

---

## 六、重要规则

1. **改前端** → 刷新浏览器即可看到效果
2. **改后端** → 必须重启服务器（ctrl+C 再 node server.js）
3. **改 CSS** → 只改 style.css，不用改其他文件
4. **改数据库结构** → 删掉 forum.db 文件，重启服务器会自动重建
5. **不要手动修改 forum.db** → 所有数据操作都通过代码完成
